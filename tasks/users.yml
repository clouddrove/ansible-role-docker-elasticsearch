---
- name: Setup group
  ansible.builtin.group:
    name: "{{ es_group }}"
    system: true


- name: Setup user
  ansible.builtin.user:
    name: "{{ es_user }}"
    system: true
    group: "{{ es_group }}"
